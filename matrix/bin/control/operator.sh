#!/bin/bash
# With the absolute directory reference, there’s no doubt as to exactly where you want to go.
# To move to a speciﬁc location in the ﬁlesystem using the absolute directory reference, you
# just specify the full pathname in the cd command:
path=$(just)
pathname=$(command)
abspath=$(reference)
dot --v