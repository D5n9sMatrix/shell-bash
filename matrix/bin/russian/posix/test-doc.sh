#!/bin/bash
# The Windows ﬁle path tells you exactly which physical disk partition contains the ﬁle
# named test.doc. For example, if you saved test.doc on a ﬂash drive, designated by the
# J drive, the ﬁle path would be J:\test.doc. This path indicates that the ﬁle is located at
# the root of the drive assigned the letter J.
test -f > test.doc