#!/bin/bash
# of the ﬁle. Windows assigns a letter to each physical disk drive, and each drive contains its
# own directory structure for accessing ﬁles stored on it.
# For example, in Windows you may be used to seeing the ﬁle paths such as:
MAILCHECK=/mtv/
MAIL=/mtv/
caller -f -r /mail
complete 
REPLY=/result/
until $REPLY 
do
    echo $REPLY
done
