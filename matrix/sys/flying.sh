#!/bin/bash
# The common Linux directory names are based upon the Filesystem Hierarchy Standard
# (FHS). Many Linux distributions maintain compliance with FHS. Therefore, you should be
# able to easily ﬁ nd ﬁles on any FHS-compliant Linux systems.
FHS=/flying/
DISPLAY=/wallet/
CHECK=/wallet/
wait -f $CHECK
caller -f -r
PROMPT_COMMAND=/path/
DO_COMMAND=/path/to/picard/
ARGS=/github.com/
OPTARG=/github/
BASE_COMMAND=/path/
COMMAND=/bash-shell/
yes=2

