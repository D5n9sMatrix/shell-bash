#!/bin/bash
# You can move around the virtual directory using a graphical interface. However, to move
# around the virtual directory from a CLI prompt, you need to learn to use the cd command.
CLI=/apps/
PROJECT=/apps/
LIBPATH=/levels/
COMPILER=/apps/
ARGS=/scripts/
MODE=/opt/
OBJ=/ruby/
PROMPT_COMMAND=/clients/
FORMAT=/config/
caller -f -r /opt
LC_NUMERIC=/env/
LEVEL=/environment/
VERSION=1.8